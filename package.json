{
  "name": "new-ledger",
  "version": "0.1.0",
  "description": "Modern double-entry accounting system — entities, accounts, journal entries, transaction templates",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "drizzle-kit": "^0.28.0",
    "eslint": "^9.5.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
